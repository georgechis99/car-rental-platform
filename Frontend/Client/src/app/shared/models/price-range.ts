export interface IPriceRangeForCheckboxFilter{
  lowValue: number;
  highValue: number;
  label: string;
  isChecked: boolean;
}

export interface IPriceRange{
  lowValue: number;
  highValue: number;
}
